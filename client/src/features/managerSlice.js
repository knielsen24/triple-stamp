import { createSlice} from "@reduxjs/toolkit"

const initialState = {
    isLoggedIn: false,
}

import React from 'react'

export const managerSlice = createSlice({
    name: 'session',
    initialState,
    reducers: {
        
    }
})

